{
    "name": "skein",
    "version": "0.1.2",
    "private": true,
    "description": "release coordination (changesets + version sync) for skein. not published.",
    "type": "module",
    "scripts": {
        "changeset": "changeset",
        "changeset:status": "changeset status",
        "version": "changeset version && make bump-version NEW_VERSION=$(node -p \"require('./package.json').version\") && npm --prefix loam install",
        "release:publish": "node scripts/release-publish.mjs"
    },
    "devDependencies": {
        "@changesets/cli": "^2.27.10"
    }
}