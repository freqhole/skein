# @freqhole/reliquary's published package.json declares a peer dependency on
# an older vitest range than loam uses; legacy-peer-deps avoids npm treating
# that mismatch as a hard install failure.
legacy-peer-deps=true
