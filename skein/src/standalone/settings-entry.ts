// dedicated tauri settings window entry point.
//
// loaded by `settings.html`. mounts the pixi settings UI full-window.

import { mountTauriSettingsWindow } from "./tauri-settings";

mountTauriSettingsWindow();
